# Tor MCP server dependencies
mcp>=1.2.0
httpx[socks]>=0.27.0
stem>=1.8.2
beautifulsoup4>=4.12.0
# Stateful, JS-capable browser routed through Tor (login / waiting-queue pages).
# C++-level fingerprinting via Camoufox (Firefox-based, anti-detection).
# After install: python -m camoufox fetch
playwright>=1.49
camoufox>=0.4.0
